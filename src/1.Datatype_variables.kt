fun main(args : Array<String>) : Unit{
    //variable declared with var are mutable can be changed
    var string : String = "Hello World"
    //variable declared with var are mutable can be changed
    val str = "Kotlin Program"

//    string = 10 //compilation error

    println(string);
    println(str)
}